{
  "name": "tech-dd-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "An AI agent that runs the first pass of a technology due diligence on a code repository.",
  "engines": { "node": ">=24" },
  "scripts": {
    "dd": "tsx src/cli.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ensemble": "file:../ensemble",
    "zod": "file:../ensemble/node_modules/zod"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  }
}
